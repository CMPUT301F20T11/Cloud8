package com.example.booktracker.control;

public interface QueryOutputCallback {
    /**
     * Callback function to be called to display the result of the query to the screen
     */
    void displayQueryResult(String result);
}
